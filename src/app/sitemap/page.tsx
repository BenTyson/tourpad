import { Map } from 'lucide-react';
import { ComingSoon } from '@/components/ui/ComingSoon';

export default function SitemapPage() {
  return (
    <ComingSoon
      title="Sitemap"
      description="A complete map of all pages and sections on TourPad."
      icon={Map}
      category="Legal"
    />
  );
}
